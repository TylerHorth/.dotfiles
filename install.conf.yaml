- defaults:
    link:
      create: true
      relink: true

- clean:
  - ~/
  - ~/.tmux/
  - ~/.config/i3/
  - ~/.config/i3status/
  - ~/.config/fish/

- link:
    ~/.vimrc: vimrc
    ~/.tmux.conf: tmux.conf
    ~/.tmux/tmuxline: tmuxline
    ~/.config/i3/config: config.i3
    ~/.config/i3status/config: config.i3status
    ~/.config/fish/config.fish: config.fish
    ~/.config/fish/promptline: promptline

- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - ["curl -fLo ~/.vim/autoload/plug.vim --create-dirs https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim; and vim +PlugInstall +qall", Installing vim plugins]
  - [fonts/install.sh, Installing powerline fonts]
